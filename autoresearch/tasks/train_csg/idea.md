# idea.md — <next run tag>

Branch: `ar-agd/<tag>` (from `autoresearch`).
Run folder: `runs/<tag>/`.

## Starting point

Establish a baseline on the default scenario (1 in cube stock, sphere target,
`--voxel-size-mm 0.5`) before varying anything. All later scenario or method
variations are compared against this baseline.

## Plan

(filled in as the run progresses)

## Notes

(filled in as the run progresses)

## Findings

(filled in as the run progresses)
